env: prod
config:
  database:
    host: "prod-db.example.com"
    ssl: true
    pool_size: 50
  queue:
    host: "prod-queue.example.com"
    cluster_mode: true
  debug: false
  monitoring: true
  
resources:
  cpu: "2000m"  # Higher resources for prod
  memory: "2Gi"
